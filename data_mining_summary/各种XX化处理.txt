-1、标准化（normalization）：数据标准化的分类有Min-max 标准化和z-score 标准化；后者是：这种方法基于原始数据的均值(mean)和标准差(standard deviation)进行数据的标准化。第三种是：小数定标规范化：小数定标规范化通过移动属性A的值的小数点位置进行规范化
0、最大最小化：
1、规范化：就是标准化
2、归一化：归一化就是要把需要处理的数据经过处理后(通过某种算法)限制在你需要的一定范围内。
3、向量化：
4、正则化(regularization)：防止过拟合，约束要优化的参数
5、二值化(binaryzation)：>100,为1，反之，为0
6、最大最小化：
7、one-hot化：
8、离散化(Discretization)：个体映射到有限的空间中，提高算法的时空效率

